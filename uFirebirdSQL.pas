unit uFirebirdSQL;
interface
procedure QueryToList(const SQL: string; LB: TObject);
implementation
uses System.SysUtils;
procedure QueryToList(const SQL: string; LB: TObject);
begin
  // Firebird bağlantısı simülasyonu
end;
end.
