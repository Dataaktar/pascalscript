unit uQRFatura;
interface
procedure GenerateQR(const FaturaNo: string; Img: TObject);
implementation
uses System.SysUtils;
procedure GenerateQR(const FaturaNo: string; Img: TObject);
begin
  // QR oluştur
end;
end.
